import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FincarreraconductorPage } from './fincarreraconductor.page';

describe('FincarreraconductorPage', () => {
  let component: FincarreraconductorPage;
  let fixture: ComponentFixture<FincarreraconductorPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(FincarreraconductorPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
